# config.toml — Tema oscuro profesional para Meme Detector Dashboard.
# Paleta coherente con la landing page (terminal/hacker aesthetic).

[theme]
primaryColor = "#00ff41"
backgroundColor = "#0a0a0a"
secondaryBackgroundColor = "#111111"
textColor = "#e0e0e0"
font = "sans serif"

[server]
headless = true
